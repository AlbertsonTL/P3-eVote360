name: CI Build & Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore eVote360.sln

    - name: Build
      run: dotnet build eVote360.sln --no-restore --configuration Release

    - name: Run Tests
      run: dotnet test eVote360.sln --no-build --configuration Release --verbosity normal
